# Fail installs on an unsupported Node version instead of only warning.
# The suites here behave differently across Node majors -- newer Node ships a
# native globalThis.localStorage that shadows the one jsdom provides, which
# silently broke 33 tests. See the "engines" field in package.json.
engine-strict=true
